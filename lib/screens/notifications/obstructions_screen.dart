import 'package:flutter/material.dart';

class ObstructionssPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Notificação - Obstruções Temporárias'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}
