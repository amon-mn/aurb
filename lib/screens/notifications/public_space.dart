import 'package:flutter/material.dart';

class UsePublicSpacePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Notificação - Uso do Espaço Público'),
          centerTitle: true,
          backgroundColor: Colors.brown,
        ),
      );
}
