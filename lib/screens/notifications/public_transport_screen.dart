import 'package:flutter/material.dart';

class PublicTransportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Notificação - Transporte Público'),
          centerTitle: true,
          backgroundColor: Colors.orange,
        ),
      );
}
