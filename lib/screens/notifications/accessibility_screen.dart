import 'package:flutter/material.dart';

class AccessibilityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Notificação - Acessibilidade'),
          centerTitle: true,
          backgroundColor: Colors.purple,
        ),
      );
}
