import 'package:flutter/material.dart';

class StreetsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Notificação - Ruas e Avenidas'),
          centerTitle: true,
          backgroundColor: Colors.green,
        ),
      );
}
