import 'package:flutter/material.dart';

class OtherNotificationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Outras Notificações'),
          centerTitle: true,
          backgroundColor: Colors.grey,
        ),
      );
}
