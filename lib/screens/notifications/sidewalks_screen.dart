import 'package:flutter/material.dart';

class SidewalksPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Notificação - Calçadas'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}
