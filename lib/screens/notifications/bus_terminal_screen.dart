import 'package:flutter/material.dart';

class BusTerminalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Notificação - Terminais de Ônibus'),
          centerTitle: true,
          backgroundColor: Colors.pink,
        ),
      );
}
